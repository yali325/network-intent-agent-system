mactav:
  agent:
    intent-mode: llm

spring:
  ai:
    model:
      chat: dashscope
    dashscope:
      api-key: ${aliApi-key:${ALI_API_KEY:}}
      chat:
        options:
          model: ${DASHSCOPE_CHAT_MODEL:qwen-plus}
          temperature: 0.2
