/**
 * Shared A2A request/response envelope contracts.
 *
 * <p>Payloads are serialized JSON so parser and validator boundaries remain
 * explicit across remote-agent calls.</p>
 */
package com.yali.mactav.model.a2a;
