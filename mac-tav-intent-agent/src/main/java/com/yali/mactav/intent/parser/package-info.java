/**
 * Parsers that convert intent response schemas into shared MAC-TAV DTOs.
 *
 * <p>Parser code runs after structured model output and before validation. It
 * must not call models, write Workspace state, or perform orchestration.</p>
 */
package com.yali.mactav.intent.parser;
