/**
 * Structured output schemas returned by the future MAC-TAV IntentAgent model call.
 *
 * <p>Schemas define the model boundary only. They are parsed into shared DTOs
 * before validation and must not carry Workspace state or provider-specific types.</p>
 */
package com.yali.mactav.intent.schema;
