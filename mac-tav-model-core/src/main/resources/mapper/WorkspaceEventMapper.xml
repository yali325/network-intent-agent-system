<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yali.mactav.modelcore.mapper.WorkspaceEventMapper">
  <insert id="insert" parameterType="com.yali.mactav.modelcore.entity.WorkspaceEventEntity">
    INSERT INTO workspace_event (
      event_id, task_id, event_type, stage, event_time, severity, title, message,
      related_artifact_id, related_record_id, trace_id, payload_summary
    ) VALUES (
      #{eventId}, #{taskId}, #{eventType}, #{stage}, #{eventTime}, #{severity}, #{title}, #{message},
      #{relatedArtifactId}, #{relatedRecordId}, #{traceId}, #{payloadSummary}
    )
  </insert>

  <select id="listByTaskId" resultType="com.yali.mactav.modelcore.entity.WorkspaceEventEntity">
    SELECT event_id, task_id, event_type, stage, event_time, severity, title, message,
           related_artifact_id, related_record_id, trace_id, payload_summary
    FROM workspace_event
    WHERE task_id = #{taskId}
    ORDER BY event_time ASC
  </select>

  <select id="listByQuery" resultType="com.yali.mactav.modelcore.entity.WorkspaceEventEntity">
    SELECT event_id, task_id, event_type, stage, event_time, severity, title, message,
           related_artifact_id, related_record_id, trace_id, payload_summary
    FROM workspace_event
    WHERE task_id = #{taskId}
    <if test="stage != null">
      AND stage = #{stage}
    </if>
    <if test="eventType != null and eventType != ''">
      AND event_type = #{eventType}
    </if>
    <if test="from != null">
      AND event_time &gt;= #{from}
    </if>
    <if test="to != null">
      AND event_time &lt;= #{to}
    </if>
    ORDER BY event_time ASC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="countByQuery" resultType="long">
    SELECT COUNT(*)
    FROM workspace_event
    WHERE task_id = #{taskId}
    <if test="stage != null">
      AND stage = #{stage}
    </if>
    <if test="eventType != null and eventType != ''">
      AND event_type = #{eventType}
    </if>
    <if test="from != null">
      AND event_time &gt;= #{from}
    </if>
    <if test="to != null">
      AND event_time &lt;= #{to}
    </if>
  </select>
</mapper>
